<div class="well sidebar-nav">
    <ul class="nav nav-list">
        <li class="nav-header">Articles connexes</li>
        {% for track in tracks %}
            {% if currentTrackId != track.id %}
                <li>
                    <a href="{{ path("fxl_music_track", {"slugProject":track.project.slug, "slugTrack": track.slug }) }}">{{ track.name }}</a>
                </li>
            {% endif %}
        {% endfor %}
    </ul>
</div>

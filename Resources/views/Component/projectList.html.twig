<div class="well sidebar-nav">
    <ul class="nav nav-list">
        <li class="nav-header">Projets connexes</li>
        {% for project in projects %}
            <li>
                <a href="{{ path("fxl_music_project", {"slug":project.slug }) }}">{{ project.name }}</a>
            </li>
        {% endfor %}
    </ul>
</div>

